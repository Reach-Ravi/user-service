apiVersion: apps/v1
kind: Deployment
metadata:
  name: spring-boot-k8s-deployment
  namespace: user-app
spec:
  selector:
    matchLabels:
      app: spring-boot-k8s
  replicas: 4
  template:
    metadata:
      labels:
        app: spring-boot-k8s
    spec:
      containers:
        - name: user-app-container
          image: user-service:1.0.0-15
          imagePullPolicy: IfNotPresent
          ports:
            - containerPort: 8081


